//这没啥说的,就延时函数
//以 ms 位单位,调用时括号中输入要延时多少 ms

void Delay(int x)	//x ms //调用Delay时输入参数
{
	int t = 0;
	for(t = 0;t < x;t ++)
			{
				unsigned char data i, j;
				i = 2;
				j = 15;
					do
						{
							while (--j);
						} while (--i);
			}
}